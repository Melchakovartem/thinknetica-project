div
 h1= - @question.title
 p= - @question.body
 = link_to "Delete", question_path(@question), method: "delete"

 - @answers.each do |a|
   p= a.body

= form_for [@question, @answer] do |f|
  = f.label :body
  = f.text_area :body
  = f.submit "Answer"


